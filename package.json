{
  "name": "safiroute",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "test": "node --test",
    "check": "node --check app.js && node --check storage.js && node --check model.js && node --check sync.js && node --check sw.js"
  }
}

